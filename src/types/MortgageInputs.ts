// input: purchase price, down payment percentage, length of loan, interest rate, exit year
export interface MortgageInputs {
  purchasePrice: number;
  downPaymentPercentage: number;
  lengthOfLoan: number;
  interestRate: number;
  exitYear: number;
}
